project:
  type: website
  output-dir: docs

website:
  title: "Open Case Study"

brand: _brand.yml

format:
  html:
    theme: cosmo
    css: styles.css
    toc: true
    toc-location: left
    include-in-header: |
      <style>
        .toc-wrapper {
          margin-top: 100px;
        }
        .brand-logo {
          position: absolute;
          top: 20px;
          left: 50%;
          transform: translateX(-50%);
          max-width: 200px;
          z-index: 1000;
        }
      </style>
